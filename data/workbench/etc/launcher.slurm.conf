# Enable debugging
enable-debug-logging=1

# Basic configuration
slurm-service-user=slurm
slurm-bin-path=/opt/slurm/bin

# GPU and GRES settings
enable-gres=1
enable-gpus=1
gpu-types=v100,p100

# Singularity specifics
constraints=Container=singularity-container

